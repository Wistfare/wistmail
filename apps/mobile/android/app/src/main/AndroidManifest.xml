<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by FCM on Android 13+ so the system shows our
         notifications. PushClient (Dart) requests this at runtime
         on first sign-in via FirebaseMessaging.requestPermission(). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for the reply RemoteInput action to call our API
         from the BroadcastReceiver. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Wimail"
        android:name=".WistmailApplication"
        android:icon="@mipmap/ic_launcher">

        <!-- Override the API base URL via gradle/manifest replacements
             for dev/staging builds. Default points at production. -->
        <meta-data
            android:name="wistmail_api_base_url"
            android:value="https://api.wistfare.com" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
              Custom-scheme deep link. Tapping wistmail://auth/reset-password?token=…
              (which we place in the password-reset email as a fallback when the
              user's mail client doesn't honour App Links) opens the app straight
              on the reset screen.

              The https App Link variant below is preferred — tapping the
              https://wistfare.com/reset-password URL in the email goes through
              Android's verified link handler and lands in the app without the
              browser chrome. Works only after the /.well-known/assetlinks.json
              file is published on the domain with the SHA-256 fingerprint of
              our signing key; documented in docs/ROADMAP-DEEP-LINKS.md.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="wistmail" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="wistfare.com"
                      android:pathPrefix="/reset-password" />
                <data android:scheme="https"
                      android:host="app.wistfare.com"
                      android:pathPrefix="/reset-password" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FCM message receiver. Replaces the default flutter_fcm
             handler so we can attach inline-reply + mark-read action
             buttons + apply AI suggestion chips on follow-up pushes.
             See WistmailFcmService.kt for the payload contract. -->
        <service
            android:name=".WistmailFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- BroadcastReceiver that handles taps on the Reply, Mark
             Read, and AI suggestion chips. Authenticates via the
             short-lived bearer token shipped in each FCM payload. -->
        <receiver
            android:name=".WistmailReplyReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
